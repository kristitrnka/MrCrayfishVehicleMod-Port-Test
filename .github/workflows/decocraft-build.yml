name: Build Decocraft Port
on:
  pull_request:
  push:
    branches: [decocraft-1.21.1-build]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.2.1'
      - name: Build Decocraft port
        working-directory: decocraft-port
        run: gradle --no-daemon build
      - uses: actions/upload-artifact@v4
        with:
          name: decocraftport-1.21.1-neoforge
          path: decocraft-port/build/libs/*.jar
