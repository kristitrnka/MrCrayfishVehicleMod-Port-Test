name: Temporary Pintonium Import

on:
  push:
    branches:
      - temp-pintonium-import-20260715
  pull_request:
    branches:
      - 1.16.X-dev
  workflow_dispatch:

jobs:
  import-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone Pintonium shader fork
        run: git clone --depth 1 https://github.com/Xplodin/Pintonium.git pintonium

      - name: Set up Java toolchains
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: |
            8
            17
            21

      - name: Use compatible Gradle and Stonecutter versions
        working-directory: pintonium
        run: |
          sed -i 's/gradle-9\.6\.0-bin\.zip/gradle-8.14.3-bin.zip/' gradle/wrapper/gradle-wrapper.properties
          sed -i 's/stonecutter_version=0\.9\.6/stonecutter_version=0.7.11/' gradle.properties

      - name: Build Forge 1.12.2 shader jar
        id: build
        working-directory: pintonium
        shell: bash
        run: |
          chmod +x gradlew
          set +e
          ./gradlew -Ptarget_versions=1.12.2 packageJar --stacktrace --console=plain > ../pintonium-build.log 2>&1
          status=$?
          echo "exit_code=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Archive source
        if: always()
        run: tar --exclude='pintonium/.git' -czf Pintonium-source.tar.gz pintonium

      - name: Show build tail
        if: always()
        run: tail -n 220 pintonium-build.log || true

      - name: Show outputs
        if: always()
        run: find pintonium -type f -name '*.jar' -print

      - name: Upload source
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pintonium-source
          path: Pintonium-source.tar.gz
          if-no-files-found: error

      - name: Upload build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pintonium-build-log
          path: pintonium-build.log
          if-no-files-found: error

      - name: Upload shader jar
        if: steps.build.outputs.exit_code == '0'
        uses: actions/upload-artifact@v4
        with:
          name: pintonium-forge-1.12.2
          path: |
            pintonium/build/libs/**/*.jar
            pintonium/forge122/build/libs/*.jar
          if-no-files-found: error

      - name: Fail if build failed
        if: steps.build.outputs.exit_code != '0'
        run: exit 1
